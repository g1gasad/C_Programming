// 1. No commas or blanks are allowed
// 2. No special symbol other than (_) is allowed
// 3. Variable names are case sensitive 
// i.e. asad and Asad will be treated as separate variables
#include <stdio.h>

int main(){
    int a; // Variable declaration
    int b;
    int c;
    int cat;
    int what_is_this;
    a = 4; // Variable Initialization
    b = 2;

    printf("The output of this program is %d and %d.", a, b);
    return 0;
}