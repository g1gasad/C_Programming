#include <stdio.h>

int main(){
    char str1[50], str2[50];
// used to input multiword strings

    gets(str1);
    gets(str2);
    // printf("%s", str);
    puts(str1);
    puts(str2);
    // printf("Wow");
    
    return 0;
}