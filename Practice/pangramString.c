/*Problem: Check if a String is a Pangram | Description:
A pangram is a sentence that contains every letter 
of the English alphabet at least once.
Takes a sentence as input (you can assume no special characters).*/
#include <stdio.h>

int main(){
    printf("%d", 'z');
    return 0;
}